package com.mycompany.javalessons.ThirdWeek.Polymorphism;

public class BaseLogger {
    // for example we will use this logger(s) in customerManager
    public void log(String message){
        System.out.println("Default Base Logger Message = " + message);
    }
}
